# LLM SDKs
anthropic>=0.40.0
openai>=1.50.0
google-generativeai>=0.8.0  # optional — for native Gemini
litellm>=1.50.0             # optional — universal abstraction

# Computer control
pyautogui>=0.9.54
mss>=9.0.1
Pillow>=10.0.0
uiautomation>=2.0.18 ; sys_platform == "win32"

# Web framework (likely already in AgentOS Pro)
fastapi>=0.115.0
pydantic>=2.7.0
python-dotenv>=1.0.0
uvicorn[standard]>=0.30.0

# Dev / tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
